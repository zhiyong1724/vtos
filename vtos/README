简介：vtos是一个微小的嵌入式内核，可以提供最基本的线程调度，内存管理，信号量，消息队列等系统服务，
该内核绝大部分用c写成，少部分跟硬件相关的用汇编编写，可以很方便地移植到不同的平台上，如果你要移植
到你的平台上，只需要修改port下的少数几个文件。
线程调度：该调度器使用了一种基于运行时间的分时调度算法，在同等优先级下，每个线程都有同等的运行时间，
区别于抢占式算法，高优先级的任务只是获得更多的运行时间，并不会抢占低优先级的任务。
内存管理：内核把内存空间划分成固定大小的块，这些块可以分拆成更小的块，也可以重新合并成大的块，这样做
不管你多少次分配和释放内存，都不会造成内存碎片化。
线程间通信：内核提供了信号量来解决线程间的同步和互斥问题，消息队列解决线程间的通信问题。
V0.0.1：第一个测试版
V0.0.2：
1、修复了部分注释错误。
2、把空闲任务的优先级调到最低级。
3、优化了代码，使得代码更加健壮。